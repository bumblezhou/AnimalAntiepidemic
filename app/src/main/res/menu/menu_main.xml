<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_search"
          android:title="@string/action_search"
          app:actionViewClass="android.support.v7.widget.SearchView"
          app:showAsAction="always"/>
    <item
        android:id="@+id/action_add_user"
        android:orderInCategory="300"
        android:title="@string/farmer_add_action_title"
        android:icon="@drawable/account_plus"
        app:showAsAction="ifRoom"></item>

    <item
        android:id="@+id/action_edit_user"
        android:orderInCategory="300"
        android:title="@string/farmer_edit_action_title"
        android:icon="@drawable/account_edit"
        app:showAsAction="ifRoom"></item>
</menu>
